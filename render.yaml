services:
  - type: web
    name: superplane-render-test
    env: static
    buildCommand: echo "Starting build..." && sleep 45 && echo "Build complete"
    staticPublishPath: .
    envVars:
      - key: TEST_ENV_VAR
        value: hello_from_superplane
